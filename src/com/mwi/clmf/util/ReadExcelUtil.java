package com.mwi.clmf.util;import java.io.FileInputStream;import java.io.InputStream;import jxl.Sheet;import jxl.Workbook;/** * jxl 读取Excel * */public class ReadExcelUtil {	//	public static String[][] readValue(String path, Integer indexSheet){//		InputStream is = null;//		String[][] value = null;//		try {//			is = new FileInputStream(path);//			Workbook wb = Workbook.getWorkbook(is);//			Sheet st = wb.getSheet(indexSheet);//			int row = st.getRows();//			int col = st.getColumns();//			value = new String[row][col];//			if(row > 0 && col > 0){//				for (int i = 0; i < row; i++) {//					for (int j = 0; j < col; j++) {//						value[i][j] = st.getCell(j,i).getContents().trim();//					}//				}//			}//			is.close();//		} catch (Exception e) {//			// TODO Auto-generated catch block//			try{//				is.close();//			}catch(Exception ex){//				ex.printStackTrace();//			}//			e.printStackTrace();//		}//		return value;//	}}